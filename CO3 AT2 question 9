#include <iostream>
#include <fstream>
using namespace std;

class FileHandler {
    ofstream file;

public:
    FileHandler() {
        file.open("data.txt");
        cout << "File Opened\n";
    }

    ~FileHandler() {
        file.close();
        cout << "File Closed\n";
    }
};

int main() {
    FileHandler f;
    return 0;
}
